import{format}from"date-fns";import{createEventAction}from"@/app/actions";import{requireSession}from"@/lib/auth";import{db}from"@/lib/db";
export default async function Events(){await requireSession("events.manage");const events=await db.event.findMany({where:{deletedAt:null},orderBy:{date:"desc"}});return <><p className="eyebrow">Multi-event platform</p><h1 className="page-title">Events</h1><details className="panel" style={{margin:"18px 0"}}><summary className="btn btn-primary">Create event</summary><form action={createEventAction} className="form-grid two" style={{marginTop:16}}><div className="field"><label>Event name</label><input className="input" name="name" required/></div><div className="field"><label>Event code</label><input className="input" name="code" required/></div><div className="field"><label>Registration prefix</label><input className="input" name="prefix" required/></div><div className="field"><label>Date</label><input className="input" name="date" type="date" required/></div><div className="field"><label>Start time</label><input className="input" name="startTime" type="time"/></div><label className="check"><input type="checkbox" name="registrationOpen"/>Registration open</label><label className="check"><input type="checkbox" name="transportEnabled"/>Transport enabled</label><label className="check"><input type="checkbox" name="soulCaptureEnabled"/>Soul capture enabled</label><button className="btn btn-primary">Create event</button></form></details><div className="table-wrap"><table><thead><tr><th>Event</th><th>Date</th><th>Prefix</th><th>Status</th><th>Registration</th></tr></thead><tbody>{events.map(e=><tr key={e.id}><td><strong>{e.name}</strong><br/>{e.code}</td><td>{format(e.date,"d MMM yyyy")}</td><td>{e.registrationPrefix}</td><td>{e.status}</td><td>{e.registrationOpen?"Open":"Closed"}</td></tr>)}</tbody></table></div></>}
