import{createUserAction}from"@/app/actions";import{requireSession}from"@/lib/auth";import{db}from"@/lib/db";
export default async function Users(){await requireSession("users.manage");const[users,roles]=await Promise.all([db.user.findMany({include:{roles:{include:{role:true}}},orderBy:{name:"asc"}}),db.role.findMany({orderBy:{name:"asc"}})]);return <><p className="eyebrow">Administration</p><h1 className="page-title">Users & roles</h1><details className="panel" style={{margin:"18px 0"}}><summary className="btn btn-primary">Add user</summary><form action={createUserAction} className="form-grid two" style={{marginTop:16}}><div className="field"><label>Name</label><input className="input" name="name" required/></div><div className="field"><label>Email</label><input className="input" name="email" type="email" required/></div><div className="field"><label>Temporary password</label><input className="input" name="password" type="password" minLength={12} required/></div><div className="field"><label>Role</label><select className="select" name="roleId">{roles.map(r=><option value={r.id} key={r.id}>{r.name.replaceAll("_"," ")}</option>)}</select></div><button className="btn btn-primary">Create user</button></form></details><div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Roles</th><th>Status</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.roles.map(x=>x.role.name.replaceAll("_"," ")).join(", ")}</td><td>{u.active?"Active":"Disabled"}</td></tr>)}</tbody></table></div></>}
